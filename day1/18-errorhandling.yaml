- name: demo on error handling
  hosts: local
  gather_facts: no
  tasks:
  - name: demo on errors
    command: id roboshop
    register: result
    ignore_errors: yes
  - name: print the result
    debug:
      var: result